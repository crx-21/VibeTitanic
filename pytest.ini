[pytest]
# Run pytest from the project root, add src/ to sys.path so `from src ...`
# imports work without installing the package, and quiet the output a touch.
testpaths = tests
pythonpath = src
addopts = -ra --strict-markers --tb=short